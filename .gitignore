# IDE / Editor
.DS_Store
.vscode/*
!.vscode/launch.json
!.vscode/tasks.json
!.vscode/extensions.json
!/.vscode/*.json

# Dependency / Build
php-src/
go.work
go.work.sum
*.log
*.test
test_bin
test_server
dist/
/jdk/

# Go binaries and build artifacts
# Ignore executable binaries (common Go build outputs)
*.exe
*.exe~
*.dll
*.so
*.dylib

# Go build cache and temporary files
*.tmp
*.swp
*.bak

# FrankenPHP specific binaries
frankenphp
frankenphp.exe
demo_app
demo2

# Build outputs directory - ignore all files but keep directory
docs/outputs/*
!docs/outputs/.gitkeep

# Submodule - Ignore content but keep the folder (managed by git submodule)
# Note: standard git submodule handling doesn't need explicit excludes usually,
# but since we had that before, let's keep it clean:
# (Removing the previous weird /frankenphp/* rules to let git submodule handle it)
